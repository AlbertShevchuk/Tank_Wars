import javafx.scene.image.Image;

class TimerObject extends GameObject
{
    TimerObject(Image img, int x, int y)            //simple calss where for the countdown timer
    {
        super(img, x, y);
    }
}
